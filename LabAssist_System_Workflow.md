# LabAssist — Workflow การทำงานของระบบ

ระบบรับสมัครผู้ช่วยสอน (TA) และผู้ช่วยห้องปฏิบัติการ (Lab Boy)

## 1. ภาพรวมสถาปัตยกรรมระบบ

ระบบประกอบด้วย 3 ส่วนหลักที่ทำงานร่วมกัน:

- **labassist-frontend** — เว็บแอปฝั่งผู้ใช้ (React + Vite + TypeScript) สำหรับนักศึกษา อาจารย์ สตาฟ และแอดมิน
- **labassist-backend** — REST API หลัก (Go + Gin) จัดการ authentication, ผู้ใช้, รายวิชา, ใบสมัคร, การแจ้งเตือน และ activity log ทุกคำขอผ่าน endpoint กลุ่ม `/api/v1`
- **labassist-ocr** — ไมโครเซอร์วิส OCR แยกต่างหาก (Python + FastAPI + EasyOCR) อ่านไฟล์ใบเกรดที่นักศึกษาอัปโหลด แล้วส่งผลกลับให้ backend

Backend เรียกใช้ OCR service ผ่าน HTTP ภายใน (proxy pattern) นักศึกษาจึงไม่ได้ติดต่อกับ OCR service โดยตรง

```
Frontend → Backend API (/api/v1) → Store/Database
                    ⇅
              OCR Service (เฉพาะขั้นตอนอ่านใบเกรด)
```

## 2. บทบาทผู้ใช้งาน (Roles)

| Role | หน้าที่ |
|---|---|
| **Student** | สมัครเป็น TA/Lab Boy, แนบใบเกรด, ติดตามสถานะใบสมัคร, รับการแจ้งเตือน |
| **Instructor** | สร้าง/จัดการรายวิชาของตนเอง, ดูผู้สมัคร, พิจารณาใบสมัคร, ส่งแจ้งเตือนผลคัดเลือก |
| **Staff** | พิจารณาใบสมัครร่วมกับอาจารย์/แอดมิน (ไม่จำกัดเฉพาะวิชาที่ตนสอน) |
| **Admin** | จัดการผู้ใช้ทั้งหมด, ดูสถิติภาพรวม, ตรวจสอบ activity log, มีสิทธิ์ทุกอย่างของ Instructor/Staff |

## 3. Flow การเข้าสู่ระบบ (Authentication)

1. **ผู้ใช้เข้าสู่ระบบ** — เลือกได้ 2 ช่องทาง: (ก) Google OAuth จำกัดเฉพาะอีเมล `@silpakorn.edu` เท่านั้น หรือ (ข) username/password สำหรับบัญชีที่แอดมินสร้างให้ (เช่น อาจารย์/สตาฟ/แอดมิน)
2. **ตรวจสอบ/สร้างบัญชี** — กรณี Google login หากยังไม่เคยมีบัญชีในระบบ จะถูกสร้างบัญชีนักศึกษาใหม่โดยอัตโนมัติ (role = student) หากมีอยู่แล้วระบบจะผูก Google ID เข้ากับบัญชีเดิม
3. **ตรวจสถานะบัญชี** — หากบัญชีถูกระงับ (`is_active = false`) จะเข้าสู่ระบบไม่ได้
4. **ออก JWT Token** — ระบุ `user_id` และ `role` แนบไปกับทุกคำขอถัดไปในรูปแบบ Bearer token
5. **แยกเส้นทางตาม role** — middleware ตรวจสอบ token และ role ในทุก endpoint ที่ต้อง login จำกัดสิทธิ์ตามบทบาท (student / instructor / staff / admin)

## 4. Flow ฝั่งนักศึกษา — สมัคร TA / Lab Boy

1. **ดูรายวิชาที่เปิดรับ** — ดูรายชื่อวิชาที่สถานะเป็น `open` หรือ `closing_soon`
2. **ส่งใบสมัคร** — เลือกวิชา + บทบาทที่สมัคร (TA หรือ Lab Boy) พร้อมเหตุผลประกอบ ระบบสร้างใบสมัครสถานะ `pending` และป้องกันการสมัครซ้ำวิชาเดิม
3. **แนบไฟล์ใบเกรด (Transcript)** — อัปโหลดไฟล์ PNG/JPG/PDF ที่หน้าโปรไฟล์ backend ส่งไฟล์ต่อไปยัง OCR service
4. **ประมวลผล OCR** — OCR service: preprocess รูปภาพ → อ่านข้อความด้วย EasyOCR → แยกรหัสวิชา/เกรด และคำนวณค่าความเชื่อมั่น (confidence) → ประเมินผลตามเกณฑ์ → ส่งเกรดที่สกัดได้ สถานะ และข้อความสรุปกลับ
5. **บันทึกผล OCR ลงโปรไฟล์** — backend บันทึกเกรด สถานะ ข้อความ และค่าความเชื่อมั่นลงในโปรไฟล์นักศึกษา ให้อาจารย์/สตาฟใช้ประกอบการพิจารณา
6. **ติดตามและถอนใบสมัคร** — ดูสถานะใบสมัครทั้งหมดได้ตลอดเวลา และถอนใบสมัคร (withdraw) ได้ก่อนมีผลตัดสินหรือแม้ผ่านการคัดเลือกแล้ว
7. **รับการแจ้งเตือน** — เมื่อผลการพิจารณาออก จะได้รับการแจ้งเตือนในระบบ ทำเครื่องหมายอ่านแล้วทีละรายการหรือทั้งหมดได้

## 5. Flow ฝั่งอาจารย์ / สตาฟ — พิจารณาใบสมัคร

1. **จัดการรายวิชา** — อาจารย์สร้าง/แก้ไขรายวิชาของตนเอง กำหนดจำนวนที่รับ (TA slots, Lab Boy slots) และเปลี่ยนสถานะวิชา (`draft → open → closing_soon → closed/archived`)
2. **ดูรายชื่อผู้สมัคร** — อาจารย์ (เฉพาะวิชาตนเอง) สตาฟ หรือแอดมิน ดูรายชื่อผู้สมัครพร้อมข้อมูลเกรด/GPA ที่ผ่าน OCR
3. **พิจารณาใบสมัคร** — อนุมัติ/ปฏิเสธทีละรายการ หรือแบบกลุ่ม (bulk review) ระบบตรวจสอบว่าที่นั่ง (slot) ยังไม่เต็มก่อนอนุมัติเสมอ
4. **ปรับจำนวนที่รับอัตโนมัติ** — เมื่อสถานะเปลี่ยนเป็น `accepted` ระบบเพิ่มจำนวนที่รับแล้วของวิชา และลดกลับหากเปลี่ยนสถานะออกจาก `accepted` หรือถูกถอน
5. **แจ้งผลให้นักศึกษา** — หลังพิจารณาเสร็จ กดส่งแจ้งเตือนไปยังนักศึกษาที่ผ่านการคัดเลือกทั้งหมดของวิชานั้นในคราวเดียว

## 6. Flow ฝั่งแอดมิน

1. **จัดการผู้ใช้** — สร้างบัญชีอาจารย์/สตาฟ/แอดมิน กำหนด role, ค้นหา/กรองผู้ใช้, เปิด-ปิดการใช้งานบัญชี
2. **ดูสถิติภาพรวม** — จำนวนผู้ใช้ นักศึกษา อาจารย์ รายวิชาที่เปิดอยู่ และใบสมัครแยกตามสถานะ (pending/accepted) แบบเรียลไทม์
3. **ตรวจสอบ Activity Log** — ทุกคำขอที่เข้าระบบถูกบันทึกผ่าน middleware กลาง กรองดูตาม user หรือ HTTP method เพื่อตรวจสอบย้อนหลังได้
4. **สิทธิ์ครอบคลุม** — แอดมินมีสิทธิ์เข้าถึงทุกฟังก์ชันของ instructor และ staff เพิ่มเติมจากสิทธิ์เฉพาะของแอดมินเอง

## 7. สถานะของใบสมัคร (Application Status)

| สถานะ | ความหมาย |
|---|---|
| `pending` | ใบสมัครเพิ่งถูกส่ง รอการพิจารณา |
| `accepted` | ผ่านการคัดเลือก — ระบบเพิ่มจำนวนที่นั่งที่รับแล้ว (ต้องไม่เกิน slots) |
| `rejected` | ไม่ผ่านการคัดเลือก |
| `withdrawn` | นักศึกษาถอนใบสมัครเอง — หากเคยผ่านการคัดเลือกแล้ว ระบบคืนที่นั่งกลับ |

## 8. Flow เจ้าหน้าที่ (Staff) — กระบวนการเอกสารขออนุมัติจ้างและเบิกจ่าย

Flow นี้เป็นกระบวนการงานเอกสาร/ธุรการที่เจ้าหน้าที่ดำเนินการนอกเหนือจากการพิจารณาใบสมัครในระบบ (ต่างจาก flow ของอาจารย์ในข้อ 5 ที่เป็นการอนุมัติ/ปฏิเสธใบสมัครในระบบโดยตรง) โดยมีขั้นตอนดังนี้:

1. **เข้าสู่ระบบและตรวจสอบแบบฟอร์ม** — เจ้าหน้าที่เข้าสู่ระบบ แล้วตรวจสอบแบบฟอร์มแจ้งความประสงค์ขอผู้ช่วยปฏิบัติการที่อาจารย์ส่งเข้ามา โดยตรวจสอบความครบถ้วนของข้อมูล
2. **ข้อมูลไม่ครบถ้วน** — เจ้าหน้าที่ส่งแบบฟอร์มกลับให้อาจารย์แก้ไขก่อน แล้ววนกลับไปตรวจสอบใหม่
3. **ข้อมูลครบถ้วน** — เจ้าหน้าที่จัดทำบันทึกข้อความขออนุมัติจ้างผู้ช่วยปฏิบัติการ
4. **สิ้นสุดภาคการศึกษา** — เจ้าหน้าที่รวบรวมและตรวจสอบหลักฐานการปฏิบัติงานของผู้ช่วยปฏิบัติการ
5. **จัดทำหลักฐานการจ่ายเงิน** — จัดทำหลักฐานการจ่ายเงินค่าตอบแทน
6. **จัดทำบันทึกขอเบิกจ่าย** — จัดทำบันทึกข้อความขอเบิกจ่ายเงิน เพื่อส่งต่อให้ฝ่ายที่เกี่ยวข้องดำเนินการด้านเอกสารและงบประมาณต่อไป

**ข้อแตกต่างจาก flow อาจารย์:** อาจารย์ทำหน้าที่พิจารณา/อนุมัติใบสมัครแต่ละคนในระบบ (accept/reject) ส่วนเจ้าหน้าที่ทำหน้าที่ด้านเอกสารธุรการ — ตรวจสอบความครบถ้วนของแบบฟอร์ม จัดทำบันทึกขออนุมัติจ้าง และดำเนินการเบิกจ่ายค่าตอบแทนเมื่อจบภาคการศึกษา

## 9. สรุป

ทุกคำขอที่ต้องยืนยันตัวตนจะผ่าน middleware ตรวจสอบ JWT และ role ก่อนถึง handler เสมอ ทำให้นักศึกษาเข้าถึงได้เฉพาะข้อมูลของตนเอง อาจารย์เข้าถึงได้เฉพาะวิชาที่ตนสอน และสตาฟ/แอดมินเข้าถึงได้กว้างกว่าตามสิทธิ์ที่กำหนด
